<%= form_for(@dip) do |f| %>
  <% if @dip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dip.errors.count, "error") %> prohibited this dip from being saved:</h2>

      <ul>
      <% @dip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :spicy %><br>
    <%= f.check_box :spicy %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
